// Dummy vendor script for the declarative `trackers` list demo. Its only job
// is to prove it got injected: it stamps a global and logs to console.
window.__trackgateDemoTrackerLoaded = true;
console.log('[demo tracker.js] loaded — consent was granted before this ran');
